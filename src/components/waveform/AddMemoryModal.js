import React, { useState } from 'react'

const AddMemoryModal = () => {
    return (
        <div id="add-memory-modal" className='modal' style={modalStyle}>
            Hello
        </div>
    )
}

const modalStyle = {
    width: '45%',
    height: '45%'
}

export default AddMemoryModal
